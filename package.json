{
  "name": "covid-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"next dev\"",
    "server":"cd server && nodemon index.js",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "concurrently": "^5.3.0",
    "next": "10.0.4",
    "nodemon": "^2.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
